---
name: note-header
description: noteの見出し画像（サムネイル）用のGPT-Image-2プロンプトを生成するスキル。`/note-header [記事の内容・テーマ]` で呼び出す。note推奨の1.91:1（1280×670px）・文字なし・参照画像前提で、登場キャラの表情・ポーズ・構図だけを記述したプロンプトを作る。登場キャラは毎回ヒアリングで決める。
---

# noteサムネ（見出し画像）プロンプト生成スキル（note-header）

noteの見出し画像用に、GPT-Image-2向けのサムネプロンプトを生成する単体スキル。
ご主人・ミア・レイン・しおりの参照画像を使う前提で、見た目の描写は省き、表情・ポーズ・構図だけを記述する。

## キャラクター情報

各キャラの参照画像はユーザーが用意する前提。プロンプト内では「N枚目の参照画像のキャラクター（キャラ名）」という形で指定する。
キャラのビジュアル詳細が必要な場合のみ、以下のCraftキャラプロンプトを参照してよい（通常は不要）。

| キャラ | キャラプロンプト ID |
|---|---|
| ご主人 | `2cc222fd-2853-4f02-8fd9-9c17bfd67520` |
| ミア | `fdd82e0f-eb09-0ee9-dd49-cbfa60d1a42f` |
| レイン | `f2690734-d804-f10b-b56e-9f3ca78092c5` |
| しおり | `f39902ea-2041-76b7-d57d-d4a6a1227f8c` |

---

## 実行手順

### STEP 0: ヒアリング（必須）

1. **記事の内容・テーマ** — `$ARGUMENTS` が空の場合は確認する。サムネのムードを決めるために、記事の雰囲気（前向き・癒し・にぎやか等）も把握する。
2. **登場キャラ** — 毎回必ず確認する。ご主人／ミア／レイン／しおり から選ぶ（複数可）。誰を何枚目の参照画像にするかも確認する。
3. **言語** — デフォルトは日本語プロンプト。希望に応じて英語も可。

### STEP 1: サムネプロンプト生成

1. **基本仕様**
   - **note推奨のアスペクト比 1.91:1（1280×670px）**。noteの見出し画像はこの比率が公式推奨。16:9（1.78:1）で作るとnote表示時に上下が少し切れるので注意。
   - **重要な要素（キャラの顔など）は画像の中央寄りに配置する**。記事一覧・プロフィール横では中央が正方形に切り抜かれて表示されるため。
   - **文字・文章・単語・ロゴは一切入れない**（GPT-Image-2は文字を入れたがるので、末尾で強めに念押しする）

2. **参照画像を使う前提のプロンプト設計**
   - ユーザーが各キャラの参照画像を用意する前提。**見た目（髪色・瞳・服など）の描写は省く**。
   - プロンプトに書くのは「参照画像ではわからない部分」だけ：**表情・ポーズ・動き・視線・構図・全体の雰囲気**。
   - **どの参照画像がどのキャラか明示する**（例：「1枚目の参照画像のキャラクター（ご主人）」「2枚目の参照画像のキャラクター（ミア）」）。配置（左右）も指定する。

3. **構図のコツ**
   - **カメラ目線になりすぎないようにする**。複数キャラなら「お互いを見て向かい合う」自然な構図を指定すると良い。
   - 各キャラに視線の向き（誰を見ているか）を書く。
   - 記事のムードを反映した雰囲気描写を加える。

4. **言語**
   - デフォルトは日本語プロンプト（ユーザー希望に応じて英語も可）。GPT-Image-2は日本語プロンプトも読める。

5. **出力フォーマット**

````
🎨 サムネ用イラスト（GPT-Image-2 / 1.91:1 (1280×670px) / 文字なし / [登場キャラ]）

```
[1.91:1・横長・雰囲気の導入文]

[シーン・場所の説明]

[N枚目の参照画像のキャラクター（キャラ名）：表情・ポーズ・視線・配置（中央寄り）]
（登場キャラぶん繰り返す）

【構図】：[向かい合わせ・カメラ目線回避・重要要素は中央寄り・全体ムード]

画像内には文字・文章・単語・ロゴを一切入れないこと。
```
````

---

## 注意事項

- STEP 0 のヒアリング（記事内容の把握・登場キャラ確認）は毎回行う。特に**登場キャラは必ず聞く**。
- プロンプトは指定言語（デフォルト日本語）、変更点の説明は日本語で行う。
- 生成後にユーザーへ確認し、構図・視線・配置などの修正要望があれば対応する。
